import 'package:flutter/material.dart';

const green = Color(0xff1ad85a);
const gr1 = Color(0xff1e130c);
const gr2 = Color(0xff9a8478);

// IconButton(
// onPressed: deneme,
// color: heartColor,
// focusColor: heartColor,
// disabledColor: heartColor,
// icon: Icon(
// FontAwesomeIcons.heart,
// ),
// ),
